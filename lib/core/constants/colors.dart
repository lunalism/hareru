import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const skyBlue = Color(0xFF4A90D9);
  static const skyBlueLight = Color(0xFFE8F2FC);
  static const skyBlueDark = Color(0xFF2A6DB5);

  // Accent
  static const sunlight = Color(0xFFFFD54F);
  static const sunlightLight = Color(0xFFFFF8E1);

  // Neutral
  static const cloud = Color(0xFFFAFAFA);
  static const cardWhite = Color(0xFFFFFFFF);
  static const night = Color(0xFF333333);
  static const nightLight = Color(0xFF888888);
  static const border = Color(0xFFEEEEEE);

  // Semantic
  static const income = Color(0xFF4CAF50);
  static const expense = Color(0xFFE57373);
  static const transfer = Color(0xFFB0BEC5);

  // Legacy aliases
  static const Color primary = skyBlue;
  static const Color accent = sunlight;
  static const Color background = cloud;
  static const Color surface = cardWhite;
  static const Color textPrimary = night;
  static const Color textSecondary = nightLight;
}
